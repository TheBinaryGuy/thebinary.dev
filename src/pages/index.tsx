import { Layout } from '@/components/index';

const Index = () => {
    return (
        <Layout>
            <h1 className='h-full grid place-items-center'>Hello, world!</h1>
        </Layout>
    );
};

export default Index;
